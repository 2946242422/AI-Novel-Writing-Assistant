# 小说正文生产执行边界

`NovelCorePipelineService` 是章节流水线任务的门面，负责创建、复用、恢复、取消任务以及维护任务生命周期。

`NovelPipelineExecutor` 负责一次已领取流水线的章节执行循环，包括正文生成、质量闭环、滚动路线补齐、下一章 JIT 预取和章节边界交接。它不创建第二套正文能力，具体章节写作、审校、修复和事实提交仍委托统一的 `ChapterRuntimeCoordinator`。

执行器内部按职责使用三个明确子边界：`qualityClosure/` 负责章节审校、质量债和明确重规划结果，`issueGovernance/` 负责把任务策略转换成继续、暂停或失败控制流，`handoff/` 只在章节安全落库边界处理生产体验交接。

维护规则：

- 自动导演、手动批量和恢复入口都通过流水线门面进入同一个执行器。
- 路线窗口只保证未来方向可用；完整执行合同只为下一章即时生成。
- 普通质量债不能停止全书生产，只有明确重规划或运行时安全问题可以停止。
- 问题事件只有在流水线状态或控制流真正采用对应动作后，才允许登记为已执行。
- 从简易自动创作交接专业工作台只能在章节落库边界生效。
- 延迟角色、世界和规划增强的优先级低于正文、审校、修复和下一章 JIT。
- `full_book_autopilot` 是无人值守生产模式，使用冻结在任务 Seed 中的预算策略。流水线在安全章节边界检查每章/每批调用和时长，超限后登记 `runtime.token_budget_exceeded` 并停止扩大范围。
- 已保存正文的恢复跳过“新正文生成”前置就绪判断，直接使用当前正文执行审校、必要修复和最终资产同步；没有正文的章节仍必须满足写作合同。
- 章节外层重试只处理可识别的瞬时传输/服务错误，不得对合同或数据校验错误盲目重放整章生产。

## AI 自主写法方案

章节完整执行合同在生成 `taskSheet`、`readerExperience` 和 `sceneCards` 时同步生成 `craftPlan`，不增加独立 LLM 调用。规划模型根据完整章节上下文自行选择 `none`、`focused` 或 `mixed` 模式，并从受控技法目录中选择 0-3 项写法；运行时不得用关键词、题材字符串或正则替模型做路由。

每项技法必须绑定当前 `sceneCards` 中真实存在的 `sceneKeys`，并携带叙事目的、执行指导和强度。`craftPlan` 只描述如何完成当前章节任务，不得增加剧情义务；如果它与章节边界、人物硬事实或义务合同冲突，后者优先。

`craftPlan` 与场景计划一起序列化到章节的 `sceneCards` JSON 中，因此不需要数据库迁移。旧章节或无法解析的旧场景数据使用 `mode=none` 的安全默认值。

正文、接收检查、审校和修复共用 required `craft_plan` 上下文块。质量链只检查已选技法的执行效果和 `avoid` 风险，不得因未选择其他技法而创建质量债；`mode=none` 时也不得要求补技巧。
